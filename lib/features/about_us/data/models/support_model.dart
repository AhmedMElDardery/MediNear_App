import 'package:flutter/material.dart';

class SupportModel {
  final String title;
  final IconData icon;
  final IconData? trailingIcon;

  SupportModel({required this.title, required this.icon, this.trailingIcon});
}
